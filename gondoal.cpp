#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
 
int main(){
 
    
        long long n;
        long long x;
        cin>>n>>x;
        vector <long long> num(n);
        for(int i=0; i<n; i++){
            cin>>num[i];
        }

        sort(num.begin(), num.end());
        
        
        
    
    
}